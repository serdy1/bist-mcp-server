services:
  - type: web
    name: bist-mcp-server
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python src/server.py
    plan: free
    envVars:
      - key: ENVIRONMENT
        value: production
